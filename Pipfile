[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pandas = ">=1.5.0"
yfinance = ">=0.2.0"
pycoingecko = ">=3.1.0"
pyarrow = ">=10.0.0"
pandas-market-calendars = ">=4.3.0"
pytz = ">=2023.3"
python-dotenv = ">=1.0.0"

[dev-packages]
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
pytest-mock = ">=3.10.0"

[requires]
python_version = "3.13"
